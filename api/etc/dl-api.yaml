Name: dl-api
Host: 0.0.0.0
Port: 8888
DLApp:
  Etcd:
    Hosts:
      - localhost:2379
    Key: dl.rpc
DataSource: root:Iwin-1-321@tcp(localhost:3306)/hello
# mysql链接地址，满足 $user:$password@tcp($ip:$port)/$db?$queries 格式即可
Table: world
Cache:
  - Host: localhost:6379